money=int(input('Введите сумму депозита:'))                    #Вводим в консоли сумму депозита

per_cent = {'ТКБ': 5.6, 'СКБ': 5.9, 'ВТБ': 4.28, 'СБЕР': 4.0}  #Словарь, содеожит инвормацию Банк: процентная ставка депозита
l=list(per_cent.values())                                      #Создаем список из процентных ставок
k=(money)*0.01                                                 #Делем наш депозит на 100

"""Делением депозита на сто, мы изменяем наш список процентных ставок в список с просто цифрами(коэфициентами)"""

g=[i*k for i in l]                                             #Каждый элемент списка l, умножаем на значение переменной k

"""i - индекс элемента списка l"""

"""Проверка"""
print(l)                                                       #Выводим на экран список l
print(k)                                                       #Выводим на экран переменную k

"""Результат"""
print(g)                                                       #Выводим на экран список g
print(max(g))                                                  #Выводим на экран наибольшее значение списка g
